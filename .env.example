# 可选高级配置（一般无需此文件，Artifactory 在 UI 设置页配置）
# 若需自定义端口或镜像地址，可复制为 .env 后修改：
#   cp .env.example .env

JF_MONITOR_PORT=8080

# 可选：使用远程多架构镜像（留空则使用 docker-compose.yml 中的 image）
# JF_MONITOR_IMAGE=your-registry/jf-monitor:latest
